const { BasePage } = require("./Base");
exports.EditContactPage = class EditContactPage extends BasePage {
  /**
   * @param {import('playwright').Page} page
   */

  constructor(page) {
    super(page);
  }
};
